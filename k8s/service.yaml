apiVersion: v1
kind: Service
metadata:
  name: demo-app-service
  namespace: devops-101
spec:
  selector:
    app: demo-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
